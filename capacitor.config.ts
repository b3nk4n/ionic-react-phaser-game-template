import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-react-phaser-game-template',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
